# Cargar v4l2loopback en el arranque -> /etc/modules-load.d/v4l2loopback.conf
#
# Asi /dev/video10 ya existe cuando abres OBS y no hay que acordarse de un
# modprobe en mitad de la clase. El modulo en reposo no consume nada.
v4l2loopback
